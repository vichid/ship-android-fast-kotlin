<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
            android:name=".App"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true">

        <activity-alias
                android:name=".Launcher"
                android:targetActivity="com.example.myapplication.launch.LaunchActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity-alias>

        <activity
                android:name="com.example.myapplication.login.LoginActivity"
                android:theme="@style/AppTheme"
                android:windowSoftInputMode="stateHidden"/>
        <activity
                android:name="com.example.myapplication.home.HomeActivity"
                android:theme="@style/AppTheme"/>
        <activity
                android:name="com.example.myapplication.ghrepositorydetail.RepositoryDetailActivity"
                android:theme="@style/AppTheme"/>
        <activity
                android:name="com.example.myapplication.launch.LaunchActivity"
                android:launchMode="singleTask"
                android:theme="@android:style/Theme.NoDisplay"
                android:windowSoftInputMode="stateHidden"/>
    </application>

</manifest>